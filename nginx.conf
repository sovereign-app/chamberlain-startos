events {}

http {
    server {
        listen 80;
        server_name caseria.clan.sovereign.app;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }
}
